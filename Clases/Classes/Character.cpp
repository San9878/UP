#include "Character.h"
#include <string>
#include <algorithm>
/*
class PlayerData : public Character {
public:
    static PlayerData* getInstance();

    // Implementaciones concretas de los métodos de Character
    int getAttackPower() const override { return playerAttack; }
    int getDefense() const override { return playerDefense; }  // Asegúrate de añadir esta propiedad si no existe
    int getHealth() const override { return playerHealth; }
    void setHealth(int health) override { playerHealth = std::max(0, health); }

    // Otros métodos específicos de PlayerData
    std::string getPlayerName() const;
    void setPlayerName(const std::string& name);
    int getPlayerLevel() const;
    void setPlayerLevel(int level);
    void addExperience(int exp);
    void addGold(int gold);

private:
    PlayerData();  // Constructor privado para el patrón Singleton

    // Variables del jugador
    std::string playerName;
    int playerLevel;
    int playerAttack;
    int playerDefense;
    int playerHealth;
    int playerExperience;
    int playerGold;

    void checkLevelUp();
    void levelUp();
};

#endif // PLAYER_DATA_H

*/
